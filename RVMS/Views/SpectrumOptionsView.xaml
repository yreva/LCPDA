<Window x:Class="RVMS.Views.SpectrumOptionsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RVMS.Views"
        mc:Ignorable="d"
        Title="Plot Options" Height="368" Width="257" ResizeMode="CanMinimize">
    <ScrollViewer>
        <StackPanel>

            <TextBlock Text="Spectrum Plot Options" FontWeight="Bold" Margin="10 5"/>
            <Rectangle Name="Blank" Margin="5 0 0 3" Fill="LightGray" Height="3"></Rectangle>

            <Grid Margin="5 0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- X Axis Limits -->
                <TextBlock Text="X Axis Limits:" Grid.Row="0" Grid.Column="0" Margin="5" Grid.ColumnSpan="2"/>
                <Button Name="AutoXButton" Grid.Row="0" Grid.Column="2" Content="   Autoscale X   " Click="OnClick_AutoX" Height="20" HorizontalAlignment="Right" Grid.ColumnSpan="2">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Green"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" BorderThickness="1" BorderBrush="Black" CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="DarkGreen"/>
                                    <Setter Property="Foreground" Value="Gray"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <TextBlock Text="Min:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
                <TextBox Name="XMin" Grid.Row="1" Grid.Column="1" Width="60" Margin="5" KeyDown="EntryBox_KeyDown" LostFocus="EntryBox_LostFocus"/>
                <TextBlock Text="Max:" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Margin="5" HorizontalAlignment="Left"/>
                <TextBox Name="XMax" Grid.Row="1" Grid.Column="3" Width="60" Margin="5" KeyDown="EntryBox_KeyDown" LostFocus="EntryBox_LostFocus"/>

                <!-- Y Axis Limits -->
                <TextBlock Text="Y Axis Limits:" Grid.Row="2" Grid.Column="0" Margin="5" Grid.ColumnSpan="2"/>
                <Button Name="AutoYButton" Grid.Row="2" Grid.Column="2" Content="   Autoscale Y   " Click="OnClick_AutoY"  Height="20" HorizontalAlignment="Right" Grid.ColumnSpan="2">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Green"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" BorderThickness="1" BorderBrush="Black" CornerRadius="5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="DarkGreen"/>
                                    <Setter Property="Foreground" Value="Gray"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>


                <TextBlock Text="Min:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="5 5 5 10"/>
                <TextBox Name="YMin" Grid.Row="3" Grid.Column="1" Width="60" Margin="5 5 5 10" KeyDown="EntryBox_KeyDown" LostFocus="EntryBox_LostFocus"/>
                <TextBlock Text="Max:" Grid.Row="3" Grid.Column="2" VerticalAlignment="Center" Margin="5 5 5 10"/>
                <TextBox Name="YMax" Grid.Row="3" Grid.Column="3" Width="60" Margin="5 5 5 10" KeyDown="EntryBox_KeyDown" LostFocus="EntryBox_LostFocus"/>
            </Grid>

            <Rectangle Margin="5 5" Fill="LightGray" Height="3"></Rectangle>

            <CheckBox Name="HoldLimits" Content="Disable Autoscale on New Spectrum" Margin="5 0 0 2"  Click="OnClick_HoldLimits"/>
            <CheckBox Name="PlotGrid" Content="Show Grid in Plot" Margin="5 0 0 2"  Click="OnChecked_ShowGrid"/>
            <CheckBox Name="ScrollEnabled" Content="Allow Mouse to Edit Axes" Margin="5 0 0 0" Click="OnChecked_MouseEvents"/>

            <Rectangle Margin="5" Fill="LightGray" Height="3"/>

            <TextBlock Text="Raw Data Line Color" HorizontalAlignment="Center" Margin="5 0 5 2"></TextBlock>
            <ComboBox x:Name="LineColorComboBox" SelectionChanged="ColorComboBox_SelectionChanged" Width="150" Height="25" Margin="0 0 0 5">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="16" Height="16" Fill="{Binding Converter={StaticResource ColorConverter}}" Margin="5 0"/>
                            <TextBlock Text="{Binding}" Margin="5 0"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <!-- Imported Spectrum Options -->
            <Rectangle Name="ImportedDivider" Margin="5 5" Fill="LightGray" Height="3" Visibility="Collapsed"/>
            <CheckBox Name="ImportedCheckBox" Content="Show Imported Spectrum" Margin="5 5 0 10" Click="OnClick_ShowImportedSpectrum" Visibility="Collapsed" HorizontalAlignment="Center"/>
            <TextBlock Name="ImportedHeader" Text="Imported Spectrum Color" HorizontalAlignment="Center" Margin="5 0 5 2" Visibility="Collapsed"/>
            <ComboBox Name="ImportedComboBox" SelectionChanged="ColorComboBox_SelectionChanged" Width="150" Height="25" Visibility="Collapsed" Margin="0 0 0 5">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Width="16" Height="16" Fill="{Binding Converter={StaticResource ColorConverter}}" Margin="5 0"/>
                            <TextBlock Text="{Binding}" Margin="5 0"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <TextBlock Name="ImportedScalerHeader" Text="Scroll to Scale Imported Data" HorizontalAlignment="Center" Margin="5 0 5 0" Visibility="Collapsed"/>
            <TextBox Name="ImportedScaler" Text="1.00" Margin="5 0 5 5" Width="100" TextAlignment="Center"  PreviewKeyDown="ImportedScaler_PreviewKeyDown" PreviewMouseWheel="ImportedScaler_PreviewMouseWheel" Visibility="Collapsed"/>

            <!-- Save and close -->
            <Rectangle Margin="5 0 5 5" Fill="LightGray" Height="3"></Rectangle>

            <Button Content="Save Settings" Click="OnClick_SaveSettings" Margin="15 5 15 5" VerticalAlignment="Bottom"/>
            <Button Content="Close Without Saving" Click="OnClick_CloseNoSave" Margin="15 0 15 5" VerticalAlignment="Bottom"/>


        </StackPanel>
    </ScrollViewer>
</Window>
