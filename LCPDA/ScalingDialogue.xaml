<Window x:Class="LCPDA.ScalingDialogue"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LCPDA"
        mc:Ignorable="d"
        Title="" Height="120" Width="200">
    <StackPanel HorizontalAlignment="Center">
        <TextBlock TextWrapping="Wrap" Width="200" TextAlignment="Center" Margin="0,10,0,10">
            Select Scaling Method
        </TextBlock>
        <Grid Margin="0,0,0,0" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="Linear" Content="Linear" Grid.Column="0" Width="80" Height="40" Margin="5 0 5 0" FontSize="16" Click="ButtonClicked"/>
            <Button x:Name="Log10" Content="Log10" Grid.Column="1" Width="80" Height="40" Margin="5 0 5 0" FontSize="16"  Click="ButtonClicked"/>
        </Grid>
    </StackPanel>
</Window>
