<Window x:Class="RawVision.Views.PeakListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RawVision.Views"
        mc:Ignorable="d"
        Title="Peak List" Height="350" Width="200">
    <ScrollViewer>
        <StackPanel>
            <TextBlock Name="Header" Margin="5"/>
            <DataGrid x:Name="dataGrid" AutoGenerateColumns="False" HeadersVisibility="Column" Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mass" Binding="{Binding Mass}" Width="*"/>
                    <DataGridTextColumn Header="Intensity" Binding="{Binding Intensity}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </ScrollViewer>
</Window>
