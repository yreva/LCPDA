<Window x:Class="LCPDA.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LCPDA"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="888">
    <Grid>
        <!-- Top Frame -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="10" HorizontalAlignment="Center">
            <Button x:Name="SelectFileButton" Content="Select File" Width="100" FontSize="12"/>
            <TextBox x:Name="FilePathTextBox" Width="550" FontSize="12" Margin="5 0 5 0"/>
            <Button x:Name="ReadFileButton" Content="Load File" Width="100" FontSize="12"/>
        </StackPanel>

        <!-- Main Content Area (for Chromatogram and Spectrum Canvas) -->
        <Grid Margin="10,50,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- First Frame (Chromatogram Canvas) -->
            <Border BorderBrush="Black" BorderThickness="1" Grid.Row="0">
                <!-- Placeholder for plot (use image or external control for actual plot) -->
                <Canvas x:Name="ChromatogramCanvas" Background="LightGray" Margin="0,0,0,0" Height="150"/>
            </Border>

            <!-- Second Frame (Settings Controls for Min RT, Max RT, etc.) -->
            <Grid Grid.Row="1" Margin="0 5 0 5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="4*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border BorderBrush="Black" BorderThickness="1" Grid.Row="0">
                    <!-- Placeholder for plot (use image or external control for actual plot) -->
                    <Canvas x:Name="SpectrumCanvas" Background="LightGray" Height="Auto"/>
                </Border>

                <StackPanel Grid.Column="2" Margin="5 5 5 5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- First row -->
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Text1" VerticalAlignment="Center" Margin="5"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Name="IntEntryBox1" Width="100" Margin="5"/>

                        <!-- Second row -->
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Text2" VerticalAlignment="Center" Margin="5"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Name="IntEntryBox2" Width="100" Margin="5"/>

                        <!-- Button that spans both columns -->
                        <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="+ 10 Scans" HorizontalAlignment="Stretch" Margin="5"/>

                        <!-- Button that spans both columns -->
                        <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Content="+ 1 Scan" HorizontalAlignment="Stretch" Margin="5"/>

                        <!-- Third row -->
                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Scan #" VerticalAlignment="Center" Margin="5"/>
                        <TextBox Grid.Row="4" Grid.Column="1" Name="IntEntryBox3" Width="100" Margin="5" Text="{Binding CurrentScanNumber}"/>

                        <!-- Button that spans both columns -->
                        <Button Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Content="- 1 Scan" HorizontalAlignment="Stretch" Margin="5"/>

                        <!-- Button that spans both columns -->
                        <Button Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Content="- 10 Scans" HorizontalAlignment="Stretch" Margin="5"/>

                        <!-- Fourth row -->
                        <TextBlock Grid.Row="7" Grid.Column="0" Text="Text4" VerticalAlignment="Center" Margin="5"/>
                        <TextBox Grid.Row="7" Grid.Column="1" Name="IntEntryBox4" Width="100" Margin="5"/>

                        <!-- Fifth row -->
                        <TextBlock Grid.Row="8" Grid.Column="0" Text="Text5" VerticalAlignment="Center" Margin="5"/>
                        <TextBox Grid.Row="8" Grid.Column="1" Name="IntEntryBox5" Width="100" Margin="5"/>
                    </Grid>
                </StackPanel>

            </Grid>

            <!-- Bottom Frame -->
            <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10">
                <Button x:Name="SaveSpectrumButton" Content="Save Spectrum Figure" Width="150" FontSize="12"/>
                <Button x:Name="SaveChromatogramButton" Content="Save Chromatogram Figure" Width="200" FontSize="12" Margin="5 0 5 0"/>
                <Button x:Name="SaveDataButton" Content="Save All Data as Text" Width="200" FontSize="12"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
